export const getTotalItems = (state) => {
    return state.filterPage.totalItems
}

export const getMinPrice = (state) => {
    return state.filterPage.minPrice
}

export const getMaxPrice = (state) => {
    return state.filterPage.maxPrice
}